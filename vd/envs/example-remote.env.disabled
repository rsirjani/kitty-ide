# TEMPLATE — point at an existing VNC endpoint (a box you already run).
# Copy to  <name>.env , fill in host/port, then:  ide-vd use <name> && ide-vd up <name>
# (`.disabled` keeps it out of `ide-vd ls`.)
provider=remote
host=127.0.0.1
port=5900
password=        # leave empty for a no-auth server
